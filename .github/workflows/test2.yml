
name: Test ACTION from JSON

on:
  workflow_dispatch:
  
jobs:
  configure:
    runs-on: ubuntu-latest
    #outputs:
      #cloud_var: ${{ steps.set-matrix.outputs.cloud_var }}
    steps:
     - name: Set environment variables
       run: |
        echo "MY_VAR=Hello, World!" >> $GITHUB_ENV
        echo "A_SECOND_VAR=Also Hello, World!" >> $GITHUB_ENV
  print:
    runs-on: ubuntu-latest
    needs: [configure]
    #outputs:
      #cloud_var: ${{ steps.set-matrix.outputs.cloud_var }}
    steps:
     - name: Print environment variables
       run: |
        echo $MY_VAR
        

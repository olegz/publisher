name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Run Web App
    runs-on: ubuntu-latest
    steps:
      - name: Build Web App
        run: |
          echo "Build Web App"
          sleep 10s
      - uses: convictional/trigger-workflow-and-wait@v1.3.0
        with:
          owner: olegz
          repo: test-one
          github_token: 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
          workflow_file_name: blank.yml
      - name: Deploy Web App
        run: |
          echo "Deploy Web App"
          sleep 10s

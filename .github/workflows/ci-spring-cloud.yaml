name: Spring Cloud Release
on:
  workflow_dispatch:
    inputs:
      your-name:
        description: 'Your name'     
        required: true
        default: Unknown   
jobs:
  #BUILD
  spring-cloud-build:
     uses: spring-cloud/spring-cloud-build/actions/workflows/ci.yaml@main
  #FUNCTION
  spring-cloud-function:
    needs: spring-cloud-build
    uses: spring-cloud/spring-cloud-function/actions/workflows/ci.yaml@main

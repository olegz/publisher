name: Test ACTION

on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    env: ${{ fromJson(`test.json`) }} # job-level env

    steps:
    - name: Print environment variable
      run: echo $ABC
